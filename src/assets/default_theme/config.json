{
  "author": "ylc395",
  "pages": {
    "index": [
      {
        "name": "menu",
        "label": "Menu",
        "inputType": "menu",
        "tip": "Navigating Menu displayed in Index Page"
      },
      {
        "name": "mainTitle",
        "label": "Main Title",
        "rules": [{ "required": true }],
        "tip": "Main Title displayed in Index Page. Probably your site name"
      },
      { "name": "subtitle", "label": "Subtitle" }
    ],
    "article": [
      {
        "name": "dateFormat",
        "label": "Date Format",
        "defaultValue": "YYYY-MM-DD HH:mm",
        "placeholder": "Default value is YYYY-MM-DD HH:mm.",
        "tip": "See <a href=\"https://momentjs.com/docs/#/displaying/\">moment.js document.</a>"
      }
    ],
    "archives": [
      {
        "name": "dateFormat",
        "label": "Date Format",
        "defaultValue": "YYYY-MM-DD HH:mm",
        "placeholder": "Default value is YYYY-MM-DD HH:mm.",
        "tip": "See <a href=\"https://momentjs.com/docs/#/displaying/\">moment.js document.</a>"
      }
    ],
    "about": [
      {
        "name": "content",
        "label": "Content",
        "inputType": "markdown",
        "rules": [{ "required": true }],
        "tip": "Introduce yourself in About Page"
      }
    ]
  },
  "siteFields": [
    {
      "name": "siteName",
      "label": "Site Name",
      "rules": [{ "required": true }],
      "tip": "Site name displayed in left-top corner of every page"
    }
  ]
}
